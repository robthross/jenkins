on:
  push:

jobs:
  compara_diretorios:   
      uses: robthross/gitactions/.github/workflows/check-template.yml@release
      with:
        ref: ${{ github.head_ref }}
        token: '${{ secrets.INTEGRACAO_TOKEN }}'